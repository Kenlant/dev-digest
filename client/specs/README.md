# client specs

Functional/behavioral specs for `@devdigest/web` — what a screen or flow must
do, distinct from the component tests colocated under `_components/`. Empty
for now; add specs here as they're written.
