# e2e docs

Deeper design docs for `@devdigest/e2e` — flow-runner architecture, agent-browser
integration notes — beyond what fits in [`../README.md`](../README.md). Empty
for now; add files here as the module accumulates design decisions worth
recording separately.
