# server specs

Functional/behavioral specs for `@devdigest/api` — what a feature must do,
distinct from the implementation tests in [`../test/`](../test/). Empty for
now; add specs here as they're written.
